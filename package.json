{
  "name": "risc-v-emulator",
  "version": "1.0.0",
  "description": "An emulator for the RISC-V instruction set architecture",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "npm run build; jest",
    "build": "tsc",
    "start": "npm run build; node ./dist"
  },
  "author": "Alice Parrish",
  "license": "ISC",
  "dependencies": {
    "immutable": "^4.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1"
  }
}
